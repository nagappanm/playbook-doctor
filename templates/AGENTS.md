# AGENTS.md

Guidance for AI coding agents working in this repository.

<!--
This file is loaded on every agent request, so keep it tight and current. It is
the single source of truth for how work is done here; CLAUDE.md is a symlink to
it so every tool reads the same rules.
-->

## Project Overview

<!-- One paragraph: what this project is, and the one or two things an agent most
needs to know before touching it. -->

## Setup

```bash
# Commands to install dependencies and prepare the environment.
```

## Verification

Run these before considering any change complete:

```bash
# The test, lint, and format commands that gate a change. An agent that cannot
# verify its own work against these is guessing.
```

## Conventions

- <!-- Commit style, branching, code layout, and anything an agent would
  otherwise get wrong. -->
